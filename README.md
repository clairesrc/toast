# breadgine

Start locally:

```bash
cd frontend
yarn
yarn start
```

Let this run in one terminal (or background it by adding `&` at the end of the command)

In another terminal:

```bash
cd backend
go run .
```
